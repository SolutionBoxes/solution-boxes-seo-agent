# Anthropic API key used to call Claude. Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional: override the Claude model used by the assistant (defaults to claude-sonnet-5).
# ANTHROPIC_MODEL=claude-sonnet-5
